function storeValue() {
    // Select the input element
    const inputElement = document.getElementById('text-box');

    // Access the value property and store it in a variable
    const inputValue = inputElement.value;
}
